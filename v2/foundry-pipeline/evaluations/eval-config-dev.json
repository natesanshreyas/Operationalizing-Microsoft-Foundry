{
  "evaluation_name": "seattle-hotel-agent-eval",
  "dataset": "datasets/seattle_hotel_eval.jsonl",
  "dataset_name": "seattle-hotel-eval-data",
  "evaluators": [
    {
      "name": "violence",
      "data_mapping": {
        "query": "${data.prompt}",
        "response": "${data.expected_output}"
      }
    },
    {
      "name": "hate_unfairness",
      "data_mapping": {
        "query": "${data.prompt}",
        "response": "${data.expected_output}"
      }
    },
    {
      "name": "protected_material",
      "data_mapping": {
        "query": "${data.prompt}",
        "response": "${data.expected_output}"
      }
    }
  ],
  "pass_threshold": 0.8
}
